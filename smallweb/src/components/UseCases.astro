---
import RotatingImage from './RotatingImage.astro';

// Configure your screenshot images here
const screenshotImages = [
  '/images/user-profile.png',
  '/images/user-profile-2.png',
  '/images/user-profile-3.png',
  // Add more image paths as needed
].filter(Boolean);
---

<section class="py-12 bg-white border-b-8 border-black relative">
  <div class="absolute top-0 right-0 w-48 h-48 bg-stripes opacity-10"></div>

  <div class="max-w-7xl mx-auto px-4">
    <div class="mb-10">
      <h2 class="text-2xl sm:text-3xl md:text-4xl font-black uppercase border-l-8 border-black pl-3 md:pl-5 mb-3">
        WHO IS THIS FOR?
      </h2>
      <p class="text-xs md:text-sm font-bold font-mono pl-3 md:pl-5 bg-pastel-mint inline-block px-3 py-1.5 border-4 border-black mt-3">
        // Different styles, same goal: Better viewing
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
      {/* THE_PLANNER */}
      <div class="relative p-5 border-4 border-black shadow-hard h-full flex flex-col bg-pastel-mint rotate-2 hover:rotate-0 transition-transform duration-200">
        {/* QR Code Corner */}
        <div class="absolute top-2 right-2 w-8 h-8 bg-black opacity-20"></div>

        <div class="w-16 h-16 bg-white border-4 border-black flex items-center justify-center mb-6 shadow-hard-sm">
          <svg class="w-8 h-8 text-black" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="3">
            <path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
          </svg>
        </div>

        <h3 class="text-base md:text-lg font-black mb-3 uppercase bg-black text-white inline-block px-2 transform -skew-x-12">
          THE_PLANNER
        </h3>

        <p class="text-sm font-bold mb-4 leading-relaxed flex-grow">
          You love control. You want to decide exactly what airs at 8:00 PM on Tuesday. Your TV guide is a masterpiece of curation.
        </p>

        <p class="text-xs font-bold mb-4 border-l-4 border-black pl-3 italic">
          "Schedule manually, full control over every time slot"
        </p>

        <div class="mt-auto pt-4 border-t-4 border-black border-dashed">
          <p class="font-mono text-xs font-bold">
            // CONTROL_FREAK_APPROVED
          </p>
        </div>
      </div>

      {/* THE_AUTOMATOR */}
      <div class="relative p-6 border-4 border-black shadow-hard h-full flex flex-col bg-pastel-pink -rotate-1 hover:rotate-0 transition-transform duration-200">
        {/* QR Code Corner */}
        <div class="absolute top-2 right-2 w-8 h-8 bg-black opacity-20"></div>

        <div class="w-16 h-16 bg-white border-4 border-black flex items-center justify-center mb-6 shadow-hard-sm">
          <svg class="w-8 h-8 text-black" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="3">
            <path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z" />
          </svg>
        </div>

        <h3 class="text-base md:text-lg font-black mb-3 uppercase bg-black text-white inline-block px-2 transform -skew-x-12">
          THE_AUTOMATOR
        </h3>

        <p class="text-sm font-bold mb-4 leading-relaxed flex-grow">
          You have 2 hours free. You don't want to think. You hit 'Auto-Schedule' and let the system serve you the next best episode.
        </p>

        <p class="text-xs font-bold mb-4 border-l-4 border-black pl-3 italic">
          "Let automation handle scheduling, sit back and watch"
        </p>

        <div class="mt-auto pt-4 border-t-4 border-black border-dashed">
          <p class="font-mono text-xs font-bold">
            // ZERO_EFFORT_REQUIRED
          </p>
        </div>
      </div>

      {/* THE_COMPLETER */}
      <div class="relative p-6 border-4 border-black shadow-hard h-full flex flex-col bg-pastel-blue rotate-1 hover:rotate-0 transition-transform duration-200">
        {/* QR Code Corner */}
        <div class="absolute top-2 right-2 w-8 h-8 bg-black opacity-20"></div>

        <div class="w-16 h-16 bg-white border-4 border-black flex items-center justify-center mb-6 shadow-hard-sm">
          <svg class="w-8 h-8 text-black" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="3">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>

        <h3 class="text-base md:text-lg font-black mb-3 uppercase bg-black text-white inline-block px-2 transform -skew-x-12">
          THE_COMPLETER
        </h3>

        <p class="text-sm font-bold mb-4 leading-relaxed flex-grow">
          Unfinished shows haunt you. You need progress bars, completion stats, and the satisfaction of marking a season 'Done'.
        </p>

        <p class="text-xs font-bold mb-4 border-l-4 border-black pl-3 italic">
          "Finish every show, track progress, achieve completion"
        </p>

        <div class="mt-auto pt-4 border-t-4 border-black border-dashed">
          <p class="font-mono text-xs font-bold">
            // 100%_COMPLETION_RATE
          </p>
        </div>
      </div>

      {/* PRIVATE_VIEWER */}
      <div class="relative p-6 border-4 border-black shadow-hard h-full flex flex-col bg-pastel-yellow -rotate-2 hover:rotate-0 transition-transform duration-200">
        {/* QR Code Corner */}
        <div class="absolute top-2 right-2 w-8 h-8 bg-black opacity-20"></div>

        <div class="w-16 h-16 bg-white border-4 border-black flex items-center justify-center mb-6 shadow-hard-sm">
          <svg class="w-8 h-8 text-black" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="3">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
          </svg>
        </div>

        <h3 class="text-base md:text-lg font-black mb-3 uppercase bg-black text-white inline-block px-2 transform -skew-x-12">
          PRIVATE_VIEWER
        </h3>

        <p class="text-sm font-bold mb-4 leading-relaxed flex-grow">
          You watch for yourself, not for your followers. You want a safe space to track your guilty pleasures without judgment.
        </p>

        <p class="text-xs font-bold mb-4 border-l-4 border-black pl-3 italic">
          "No social media, just personal viewing, total privacy"
        </p>

        <div class="mt-auto pt-4 border-t-4 border-black border-dashed">
          <p class="font-mono text-xs font-bold">
            // NO_WITNESSES
          </p>
        </div>
      </div>
    </div>

    {/* App Screenshot Placeholder */}
    <div class="mt-12 relative max-w-4xl mx-auto">
      <div class="absolute -top-4 -right-4 bg-black text-white px-3 py-1 text-xs font-bold font-mono z-10 rotate-3">
        SCREENSHOT_002
      </div>
      <div class="border-4 border-black bg-brutal-gray p-8 shadow-hard transform -rotate-1">
        <div class="aspect-video bg-white border-4 border-black overflow-hidden">
          {screenshotImages.length > 0 ? (
            <RotatingImage
              images={screenshotImages}
              alt="User Profile & Analytics View"
              interval={4000}
              class="border-4 border-black shadow-hard"
            />
          ) : (
            <div class="w-full h-full border-4 border-dashed border-black flex items-center justify-center">
              <div class="text-center">
                <div class="text-5xl mb-4">📊</div>
                <p class="font-bold text-sm">Add images to /public/images/</p>
                <p class="font-mono text-xs mt-2">user-profile.png, etc.</p>
              </div>
            </div>
          )}
        </div>
      </div>
    </div>
  </div>
</section>
